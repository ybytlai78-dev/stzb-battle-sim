# 率土之滨式 SLG 战斗模拟引擎

> 把一款商业 SLG 的回合制战斗机制**完整逆向、建模**，实现为确定性、可测试的纯逻辑 TypeScript 引擎，并提供浏览器直接试玩的模拟器。

**[▶ 点这里在线试玩](https://ybytlai78-dev.github.io/stzb-battle-sim/)** — 免安装、免注册、免登录，手机浏览器直接开

---

## 给玩家：这是干什么用的

一个网页版的**配将推演工具**。不用下载，不用注册，打开就能用：

- **自由配将** —— 75 名已上架武将随你挑（库中登记 161 名五星：主战法未实现的 49 名、受谋略/受攻击成长率待确认的 37 名暂不下放）。支持按势力（汉 / 魏 / 蜀 / 吴 / 群 / 晋）、兵种（骑 / 步 / 弓）筛选，也可以直接搜名字
- **三对三模拟** —— 红蓝双方各三人，安排到**大营 / 中军 / 前锋**。点一下「开始模拟」，跑满 8 回合给出战报
- **三种视角看战报** —— 简略战报看结果、详细战报看过程、统计看数据。逐回合显示谁先出手、打了谁、打出多少伤害、兵力怎么掉的
- **伤害测试实验室** —— 把对手换成一个**参数随便调的靶子**（攻击 / 防御 / 谋略 / 速度 / 兵力都可改），专门用来验证「这套战法到底能打出多少伤害」
- **战法速查** —— 230 个战法的完整描述、类型、攻击距离、发动率

**能拿来干嘛：**

- 配将之前先推演一把，少走弯路
- 跟盟友争论「谁更强」时，找个不带情绪的裁判
- 想知道某个战法在数值上到底有多离谱 —— 拉进实验室，把靶子拧到极限看数字
- 想知道「换个红度/加点会差多少」 —— 改完立刻重跑

**同一场战斗可以复现**：战报里会给出随机种子，同一种子永远得出完全一样的结果。所以一场战斗是可以被反复讨论、复盘的，不是看运气的一次性产物。

---

## 界面预览

### 配将

选武将、看势力兵种、安排位置。

![配将界面](docs/screenshots/main.png)

### 战报

兵力条、每将剩余兵力、逐回合行动明细，能切换到统计与详细视图。

![战报](docs/screenshots/report.png)

### 伤害测试实验室

对手是一个可自由调参的靶子，用来做单点数值验证。

![伤害测试实验室](docs/screenshots/damage-lab.png)

---

## 给开发者：这是什么

项目由两层组成，职责严格分离：

| 层 | 位置 | 说明 |
|----|------|------|
| **战斗引擎** | `src/engine` `src/data` | 纯逻辑，零 UI / 框架依赖，可被任意客户端调用。所有随机数经确定性 RNG，同种子 → 逐字节一致的战报 |
| **Web 模拟器** | `web/` | 基于引擎的前端：配将 → 回合制模拟 → 简略战报 / 详细战报 / 统计面板 / 伤害测试实验室 |

目标不是做一个"看起来像"的模拟器，而是**把战斗公式与机制还原到可被单元测试验证的程度**。

### 项目规模（实测）

| 指标 | 数量 |
|------|------|
| 武将数据 | **161** 名（含立绘资源）｜其中**上架可玩 75** 名（主战法未实现 49 / 成长率待确认 37） |
| 战法数据 | **230** 个（类型 / 攻击距离 / 发动率 / 目标 / 成长率） |
| 测试 | **118 个测试文件 · 1,284 个测试用例，全绿** |
| 代码量 | **44,310 行**（TypeScript / CSS / HTML / 脚本） |
| 引擎内核 | 12,093 行（`src/engine` + `src/data`） |
| 机制调研文档 | `dateyuan/` 下 **12 份**（24.6 万字符 / 2,919 行） |

---

## 技术要点

### 1. 确定性战斗（无隐藏随机）

引擎内所有随机只经 `src/engine/rng.ts`（mulberry32 种子 RNG）。
**任何将 `Math.random` 引入引擎的改动都视为错误** —— 这是硬约束，不是建议。

同一种子重复运行 → 完全相同的事件流与战报，因此回归测试可以断言到具体伤害数值。

### 2. golden 快照测试

`tests/__snapshots__/golden.json` 锁定全部固定测试集的完整战报输出。
引擎行为一旦发生非预期变化，快照立即失败；确认为预期变更后重新生成即可。

这让"重构引擎"这件事从"凭手感"变成"红绿灯"。

### 3. 事件流架构

```
combat.ts   主循环 → 生成事件流
   ├─ report.ts   渲染 JSON / 文本战报
   └─ stats.ts    从事件流汇总普攻 / 战法 / 治疗统计
```

引擎只产出事件，渲染与统计各自消费 —— 新增一种输出不需要改战斗逻辑。

### 4. 战法系统（discriminated union 建模）

5 类战法：`SimpleActive` / `PreparedActive`（1 回合准备）/ `Pursuit` / `Command` / `Passive`，均带 `tags: EffectTag[]` 供冲突判定。

指挥战法的差异被显式建模，而非用 if 堆出来：

| 字段 | 含义 |
|------|------|
| `phase` | `prep` 一类指挥（准备阶段释放一次）/ `round` 二类指挥（正式回合判定） |
| `roundRepeat` | 预备负面效果按回合判定（如战必断金前 3 回合 90%） |
| `delayedOutput` | 延迟结算：准备阶段按当时兵力/属性预存伤害，到回合直接打出 |
| `retainAfterDeath` | 施法者阵亡后效果仍生效 |
| `dynamicTriggerRate` | 动态发动率（未生效则递增，生效后重置） |
| `priorityRounds` | 前 N 回合优先行动 |

### 5. 战斗流程

```
准备阶段：速度排序 → 一类指挥战法（释放一次，按准备时兵力/属性结算）
每回合：  优先行动单位先手 → 其余按速度/站位排序 → 逐个行动
单将行动：被动 → 指挥预备判定 → 二类指挥判定 → 混乱检查 → 怯战检查
          → 准备战法检查 → 主动战法 → 普通攻击（连击至多 2 次）→ 追击战法
回合结束：状态递减 / 移除
```

同速时站位先手：前锋 > 中军 > 大营。

---

## 快速开始

```bash
npm install

npm test                      # 全部测试（1,284 个测试用例）
npm run dev                   # 跑默认测试集，输出文本战报
npm run dev -- T1_PURE_ATTACK # 指定内置测试集
npx tsc --noEmit              # 类型检查（strict）

npm run web                   # 启动 Web 模拟器（Vite，本地开发）
npm run web:build             # 构建产物
```

CLI 战报为**真实输出**，伤害按来源拆解，便于核对公式：

```
======== 战斗开始 ========
种子 10001 ｜ 最多 8 回合
我方：前锋 普攻测试将
敌方：前锋 木桩前锋 / 中军 木桩中军 / 大营 木桩大营

────────── 准备阶段 ──────────
> 出手顺序：t1 > enemy-front > enemy-mid > enemy-back
【阵容】
  → 木桩大营 阵营加成：攻击+5  谋略+6  防御+8  速度+2
  → 木桩大营 兵种加成：攻击+5  防御+8
【兵种】
  → 暂无效果
【战法】
> 准备阶段结束

────────── 第 1 回合 ──────────
> 出手顺序：t1 > enemy-front > enemy-mid > enemy-back
[行动] 普攻测试将（前锋）· 普通攻击
  → 普攻命中「enemy-mid」（距离2）造成 564（兵力基础211 + 属性基础51 + 主要302）
[行动] 木桩前锋（前锋）· 普通攻击
  → 普攻命中「t1」（距离1）造成 392（兵力基础211 + 属性基础23 + 主要158）
[行动] 木桩大营（大营）· 普通攻击
  → 木桩大营 无法普攻：攻击距离 2 内无存活敌军
[回合结束] 我方兵力 9,227（伤兵 734 阵亡 39）｜ 敌方兵力 10,000/9,436/10,000
```

---

## 项目结构

| 路径 | 说明 |
|------|------|
| `src/engine/` | 战斗内核：主循环、战法结算、目标选择、伤害公式、状态、随机数 |
| `src/data/` | 武将面板、战法定义、上架清单、称号 |
| `src/cli.ts` | 命令行入口，输出文本 / JSON 战报 |
| `web/` | Web 模拟器（配将、战报、伤害测试实验室） |
| `tests/` | 91 个测试文件 + golden 快照 + 固定测试集 fixtures |
| `dateyuan/` | 机制调研文档（数据权威来源） |
| `docs/` | 待补充机制清点、下架武将清单、开发公约 |
| `scripts/` | 数据生成、导出、资源下载、部署脚本 |
| `public/` | PWA manifest、Service Worker、立绘与图标资源 |

---

## 数据来源与研究方法

**所有数值都来自调研文档，不编造。** `dateyuan/` 下 12 份文档记录了每个战法、每张公式的推导与交叉验证过程：

- `战斗伤害公式调研.md` — 伤害构成与各项系数
- `谋略战法受谋略成长调研.md` / `速度战法受速度成长调研.md` — 成长率来源
- `七将主战法调研.md` / `司马家族武将调研.md` / `XP姜维调研.md` — 单武将机制拆解
- `武将数据调研.md` / `批量武将入库调研.md` — 面板值与入库流程
- `通用战法调研.md` — 通用战法数值
- `战斗系统设计文档.md` / `用户故事.md` — 整体设计与决策记录

当社区资料与官方描述冲突时，以多源交叉验证的结论为准，并在文档中记录取舍理由。

---

## 已知边界（诚实说明）

- 部分通用战法因机制未建模而暂未录入，缺口在 `docs/待补充机制清点.md` 中按缺失机制分组列出
- 武将池上架口径以 `src/data/listing.ts` 为准（须主战法已实现、且受谋略/受攻击成长率已确认）：161 名五星记录 → **上架 75 名**（主战法未实现 49 + 成长率待确认下架 37）
- 下架武将 / 战法清单见 `docs/下架武将清单.md`（含卡点与缺失机制分布），成长率待补见 `docs/成长率待补名单.md`
- 初版只做五星武将，四星未入库
- 引擎严格按调研文档实现；社区模拟器的简化处理（如恢复率 floor 截断）**不予采用**

---

## 部署

```bash
bash scripts/deploy-gh-pages.sh
```

构建产物发布到 `gh-pages` 分支 → <https://ybytlai78-dev.github.io/stzb-battle-sim/>

---

## 免责声明

本项目为**非官方**的个人技术研究与学习作品，不提供任何游戏服务，与网易及其关联公司无任何关系。

游戏名称、武将名称、战法描述及立绘素材的版权归 **《率土之滨》及其权利人** 所有，本项目仅在研究与学习目的下引用。如有侵权，请联系删除。

---

## 技术栈

`TypeScript`（strict）· `Vite` · `Vitest` · `tsx` · `MySQL`（数据入库）· `PWA`（Service Worker + manifest）

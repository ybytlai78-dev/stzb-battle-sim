# 拆解通用 B 级以上 · 第二阶段兵种阵型 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补兵种过滤与普攻/主动/追击/策略增减伤分流，完整入库方圆 / 疏数 / 衡轭 / 锋矢 / 鱼鳞 / 鹤翼 / 白刃 / 全军突击 / 飒沓如星。

**Architecture:** 不抽阵法类型、不按战法名特判。在 `BaseSkill` / `SkillOutput` / `damage_boost` 上加可选字段；`damageBoosts` 带本次伤害上下文跳过不匹配条目。缺省保持旧行为。

**Tech Stack:** TypeScript 引擎、Vitest、`SKILL_REGISTRY` / `SKILL_ID_BY_NAME`。

## Global Constraints

- Spec：`docs/superpowers/specs/2026-09-15-拆解通用B级以上-兵种阵型-design.md`
- 禁止 `Math.random` 进入引擎；随机只走 `rng.ts`
- JSDoc 用中文
- 完整实现，不做部分实现
- 受谋略 / 受防御 / 受速度无 `growthRate` 时用满级基值（现有 `defenseScaled && growthRate !== undefined` 分支已会 fallback 到原 rate）
- 本批只做上述 9 个战法；不做第三阶段（attackScaled / 不攻 / 万箭齐发 / 文伐 / 恃强淬锋）
- 不改 Web UI；不把本批挂进 T1～T9；无误伤则不重生 golden
- 文案以 `scripts/skill_extra.json` 满级 `desc` 为准
- **不要 git commit**（除非用户在本会话明确要求提交）；跳过所有 Commit 步骤
- 就地实现（当前 `main` 工作区）
- 分兵的 `applyDamage` 第 6 参保持 `'skill'`（回马/健卒不吃分兵）；**增减伤上下文**把分兵当 `damageSource:'basic'`（方圆普攻减伤吃分兵）

## File Structure

| 文件 | 职责 |
|------|------|
| `src/engine/types.ts` | `teamTroopFilter`；输出 `troopTypes`；增减伤维度；`split.charges`；`oddRounds`；`attacker:'recipient'` |
| `src/engine/action.ts` | 阵容门闩、目标滤兵种、`DamageHitContext`、分兵 charges、奇数回合、代打 |
| `src/data/skills.ts` | 9 个战法 |
| `scripts/build_heroes_seed.mjs` | `SKILL_ID_BY_NAME` |
| `scripts/_classified.json` | 9 条 skipped → implemented |
| `tests/troop_filter.test.ts` | 引擎单测 |
| `tests/universal_b_plus_p2.test.ts` | 每战法 3 测 |
| `docs/待补充机制清点.md` | skipped 69→60；删 troop_type 7 个与 troop_next 2 个 |

---

### Task 1: 类型增量

**Files:**
- Modify: `src/engine/types.ts`

**Interfaces:**
- Consumes: `BaseSkill` / `SkillOutput` / `CreateStatus` / `Status` / `CommandSkill.roundStartRepeat`
- Produces: 下方字段。`applyDamage` 签名本任务不改。

- [ ] **Step 1: `BaseSkill` 加 `teamTroopFilter`**

在 `groupCount` 后追加：

```ts
  /**
   * 开场上阵单位的 troopType 集合必须 ⊆ 此列表，否则本战法整次不生效（疏数弓+骑）。
   * 读部署名单（不论 alive）；战斗中不再复查。
   */
  teamTroopFilter?: TroopType[];
```

- [ ] **Step 2: 输出加 `troopTypes`；`physical_damage.attacker` 扩 `'recipient'`；加 `range`**

`physical_damage` 的 `attacker` 改为 `'lowest_strategy_ally' | 'recipient'`。JSDoc：`'recipient'` = 以每个通过兵种过滤的目标为攻击者，按其攻击/兵力/造成侧增减伤，对距离 `range`（缺省战法 range）内敌军单体打物理；杀伤 sourceId 为该单位。

同段加：

```ts
      /** 代打选敌距离（疏数骑兵 3）；缺省 skill.range */
      range?: number;
      /** 只对这些兵种的当前目标池结算 */
      troopTypes?: TroopType[];
```

`inflict_status` / `remove_debuffs` / `grant_damage_boost` 各加 `troopTypes?: TroopType[]`。`remove_debuffs` 改为对象：

```ts
  | { kind: 'remove_debuffs'; target?: 'self'; troopTypes?: TroopType[] }
```

现有 `{ kind: 'remove_debuffs' }` 与 `{ kind: 'remove_debuffs', target: 'self' }` 仍合法。

- [ ] **Step 3: `damage_boost` / `damage_reduce` / `split` 增量；`roundStartRepeat.oddRounds`**

`CreateStatus` 的 `damage_boost` 在 `chargesStack` 旁加：

```ts
    damageSource?: 'basic' | 'skill';
    skillTypes?: SkillType[];
    damageType?: 'physical' | 'strategy';
```

`damage_reduce` 同样加这三项（鱼鳞也可用 taken boost，字段对称留给减伤）。

`Status` 的 `damage_boost` / `damage_reduce` 拷贝这三项。

`CreateStatus` 与 `Status` 的 `split` 加 `charges?: number` 与 `strategyScaled?: boolean`。有 `charges` 时不按回合递减。

`roundStartRepeat` 加 `oddRounds?: boolean`。

- [ ] **Step 4: `npx tsc --noEmit`**

Expected: PASS（只加可选字段）。

跳过 Commit。

---

### Task 2: 阵容门闩 + 目标兵种过滤

**Files:**
- Modify: `src/engine/action.ts`（`triggerCommandSkills` 约 198 行前；`executeSkillWithTargets` 约 2810 行前；`executeSkillOutputs` 组好 `pool` 之后）
- Create: `tests/troop_filter.test.ts`

**Interfaces:**
- Consumes: `skill.teamTroopFilter`；`out.troopTypes`
- Produces: `export function teamPassesTroopFilter(team: UnitState[], allowed: TroopType[]): boolean`

- [ ] **Step 1: 写失败测试**

`tests/troop_filter.test.ts` 复用 `tests/on_hurt_p1.test.ts` 的 `dummyUnit` / `makeCtx`（完整拷贝，含 `hasActedThisRound`）。再加：

```ts
import { teamPassesTroopFilter, triggerCommandSkills } from '../src/engine/action';

describe('teamPassesTroopFilter', () => {
  it('集合 ⊆ 允许列表则通过；混入第三兵种失败；3 弓算仅弓+骑', () => {
    const a = dummyUnit('a', '前锋', { troopType: 'archer' });
    const c = dummyUnit('c', '中军', { troopType: 'cavalry' });
    const i = dummyUnit('i', '大营', { troopType: 'infantry' });
    expect(teamPassesTroopFilter([a, c], ['archer', 'cavalry'])).toBe(true);
    expect(teamPassesTroopFilter([a, a, a], ['archer', 'cavalry'])).toBe(true);
    expect(teamPassesTroopFilter([a, c, i], ['archer', 'cavalry'])).toBe(false);
  });

  it('阵亡不改集合', () => {
    const a = dummyUnit('a', '前锋', { troopType: 'archer' });
    const inf = dummyUnit('i', '中军', { troopType: 'infantry' });
    inf.alive = false;
    expect(teamPassesTroopFilter([a, inf], ['archer', 'cavalry'])).toBe(false);
  });
});

describe('teamTroopFilter 指挥整法跳过', () => {
  it('混步兵时疏数不发 skill_cast、弓兵无防御 buff', () => {
    const skill: Skill = {
      id: 'test_ss', name: '疏数', type: 'command', phase: 'prep', range: 2, triggerRate: 1,
      targetMode: 'group', groupCount: 3, targetSide: 'ally', tags: ['buff_defense'],
      teamTroopFilter: ['archer', 'cavalry'],
      output: [{ kind: 'inflict_status', troopTypes: ['archer'], status: { type: 'defense_buff', amount: 50, duration: 999 } }],
    };
    const caster = dummyUnit('caster', '前锋', { troopType: 'archer', commandSkillIds: ['test_ss'] });
    const mid = dummyUnit('mid', '中军', { troopType: 'cavalry' });
    const back = dummyUnit('back', '大营', { troopType: 'infantry' });
    const foe = dummyUnit('foe', '前锋', {}, 'enemy');
    const ctx = makeCtx([caster, mid, back], [foe]);
    ctx.skills.set('test_ss', skill);
    triggerCommandSkills(ctx, caster);
    expect(ctx.events.filter((e) => e.type === 'skill_cast' && e.skillId === 'test_ss')).toHaveLength(0);
    expect(caster.statuses.filter((s) => s.type === 'defense_buff')).toHaveLength(0);
  });

  it('弓+骑上阵则弓兵吃防御，骑兵不吃', () => {
    const skill: Skill = {
      id: 'test_ss', name: '疏数', type: 'command', phase: 'prep', range: 2, triggerRate: 1,
      targetMode: 'group', groupCount: 3, targetSide: 'ally', tags: ['buff_defense'],
      teamTroopFilter: ['archer', 'cavalry'],
      output: [{ kind: 'inflict_status', troopTypes: ['archer'], status: { type: 'defense_buff', amount: 50, duration: 999 } }],
    };
    const caster = dummyUnit('caster', '前锋', { troopType: 'archer', commandSkillIds: ['test_ss'] });
    const mid = dummyUnit('mid', '中军', { troopType: 'cavalry' });
    const back = dummyUnit('back', '大营', { troopType: 'archer' });
    const foe = dummyUnit('foe', '前锋', {}, 'enemy');
    const ctx = makeCtx([caster, mid, back], [foe]);
    ctx.skills.set('test_ss', skill);
    triggerCommandSkills(ctx, caster);
    expect(ctx.events.some((e) => e.type === 'skill_cast' && e.skillId === 'test_ss')).toBe(true);
    expect(caster.statuses.some((s) => s.type === 'defense_buff' && s.amount === 50)).toBe(true);
    expect(mid.statuses.some((s) => s.type === 'defense_buff')).toBe(false);
    expect(back.statuses.some((s) => s.type === 'defense_buff')).toBe(true);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/troop_filter.test.ts`

Expected: FAIL（`teamPassesTroopFilter` 未导出）。

- [ ] **Step 3: 实现**

`src/engine/action.ts` 导出：

```ts
/**
 * 开场上阵兵种集合是否 ⊆ allowed（不论 alive）。
 * @param team 该侧部署名单
 * @param allowed 允许的兵种
 */
export function teamPassesTroopFilter(team: UnitState[], allowed: TroopType[]): boolean {
  const set = new Set(team.map((u) => u.general.troopType));
  for (const t of set) {
    if (!allowed.includes(t)) return false;
  }
  return set.size > 0 || allowed.length === 0;
}
```

`triggerCommandSkills`：在 `if (targets.length === 0) continue;` 之后、`skill_target` 之前：

```ts
    if (skill.teamTroopFilter && !teamPassesTroopFilter(
      unit.side === 'my' ? ctx.myTeam : ctx.enemyTeam,
      skill.teamTroopFilter
    )) continue;
```

`executeSkillWithTargets`：在 `if (targets.length === 0) return;` 之后、`skill_cast` 之前同样检查（用 `unit` 的侧）。失败则 return，不发 `skill_cast`。

`executeSkillOutputs` 在 `let pool = ...` 与 `onlyIfOverlapPrevious` 处理之后、`switch` 之前：

```ts
    if ('troopTypes' in out && out.troopTypes && out.troopTypes.length > 0) {
      pool = pool.filter((u) => out.troopTypes!.includes(u.general.troopType));
    }
```

- [ ] **Step 4: 跑测试**

Run: `npx vitest run tests/troop_filter.test.ts`

Expected: PASS。

`npx tsc --noEmit` PASS。

跳过 Commit。

---

### Task 3: 增减伤按本次伤害过滤

**Files:**
- Modify: `src/engine/action.ts`（`damageBoosts`、`collectDamageModifiers`、所有 `damageBoosts(` 调用点、`sumRates(..., 'damage_reduce')` 与增减伤同行的路径）
- Modify: `tests/troop_filter.test.ts`

**Interfaces:**
- Consumes: `Status.damageSource` / `skillTypes` / `damageType`
- Produces: `export type DamageHitContext`；`export function statusMatchesHit(...)`

调用点与上下文（必须逐个改，缺省 `hit` 不传 = 不过滤，旧单测不破）：

| 调用 | hit |
|------|-----|
| `dealAttack` ~2962 | `{ damageSource:'basic', damageType:'physical' }` |
| `executeSplitAttack` ~1137 | `{ damageSource:'basic', damageType:'physical' }` |
| `executeSkillOutputs` physical_damage ~2343 | `{ damageSource:'skill', damageType:'physical', skillType: skill.type }` |
| `executeSkillOutputs` strategy_damage ~2433 | `{ damageSource:'skill', damageType:'strategy', skillType: skill.type }` |
| positional / 奇兵 ~698 | `{ damageSource:'skill', damageType:'physical', skillType:'command' }` |
| DoT `computeDotTickDamage` ~964 | `{ damageSource:'skill', damageType:'strategy', skillType: 施法者战法 type 若能解析 }` |
| 反击 `settleCounterOnHurt` ~3406 | `{ damageSource:'skill', damageType:'physical', skillType:'command' }` |

`collectDamageModifiers` 加同一可选 `hit`，过滤 caused/taken/reduce 列表。

- [ ] **Step 1: 写失败测试**

本任务只测 `statusMatchesHit`（不导出 `damageBoosts`）。方圆/锋矢伤害对比放 Task 6。追加到 `tests/troop_filter.test.ts`：

```ts
describe('statusMatchesHit', () => {
  const basic = { damageSource: 'basic' as const, damageType: 'physical' as const };
  const activePhys = { damageSource: 'skill' as const, damageType: 'physical' as const, skillType: 'active' as const };
  const pursuitPhys = { damageSource: 'skill' as const, damageType: 'physical' as const, skillType: 'pursuit' as const };
  const strat = { damageSource: 'skill' as const, damageType: 'strategy' as const, skillType: 'active' as const };

  it('缺省不过滤', () => {
    expect(statusMatchesHit({ rate: 0.1 } as never, basic)).toBe(true);
    expect(statusMatchesHit({}, activePhys)).toBe(true);
    expect(statusMatchesHit({}, undefined)).toBe(true);
  });

  it('basic 不匹配 skill 物理', () => {
    expect(statusMatchesHit({ damageSource: 'basic' }, basic)).toBe(true);
    expect(statusMatchesHit({ damageSource: 'basic' }, activePhys)).toBe(false);
  });

  it('skillTypes active+pursuit 吃主动与追击、不吃指挥', () => {
    const s = { damageSource: 'skill' as const, skillTypes: ['active', 'pursuit'] as SkillType[] };
    expect(statusMatchesHit(s, activePhys)).toBe(true);
    expect(statusMatchesHit(s, pursuitPhys)).toBe(true);
    expect(statusMatchesHit(s, { damageSource: 'skill', damageType: 'physical', skillType: 'command' })).toBe(false);
    expect(statusMatchesHit(s, basic)).toBe(false);
  });

  it('仅 active 不吃追击', () => {
    const s = { damageSource: 'skill' as const, skillTypes: ['active'] as SkillType[] };
    expect(statusMatchesHit(s, activePhys)).toBe(true);
    expect(statusMatchesHit(s, pursuitPhys)).toBe(false);
  });

  it('damageType strategy 不吃普攻', () => {
    expect(statusMatchesHit({ damageType: 'strategy' }, strat)).toBe(true);
    expect(statusMatchesHit({ damageType: 'strategy' }, basic)).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测确认失败**

Run: `npx vitest run tests/troop_filter.test.ts`

Expected: FAIL（`statusMatchesHit` 未导出）。

- [ ] **Step 3: 实现过滤并接到所有 calc 路径**

```ts
export type DamageHitContext = {
  damageSource?: 'basic' | 'skill';
  damageType?: DamageType;
  skillType?: SkillType;
};

/**
 * 增减伤/减伤是否计入本次伤害。hit 缺省或某维缺省 = 该维不限制。
 */
export function statusMatchesHit(
  s: { damageSource?: 'basic' | 'skill'; skillTypes?: SkillType[]; damageType?: 'physical' | 'strategy' },
  hit?: DamageHitContext
): boolean {
  if (!hit) return true;
  if (s.damageSource && hit.damageSource && s.damageSource !== hit.damageSource) return false;
  if (s.damageType && hit.damageType && s.damageType !== hit.damageType) return false;
  if (s.skillTypes && s.skillTypes.length > 0) {
    if (!hit.skillType || !s.skillTypes.includes(hit.skillType)) return false;
  }
  return true;
}
```

`damageBoosts` 增加第 4 参 `hit?: DamageHitContext`。`causedBoost` / `takenBoost` 在 `sumRates` 前先 `filter` `statusMatchesHit`。

对 `damage_reduce`：不要改 `formulas.sumRates` 签名。在 action 里：

```ts
function sumReduce(target: UnitState, hit?: DamageHitContext): number {
  const list = target.statuses.filter(
    (s) => s.type === 'damage_reduce' && statusMatchesHit(s, hit)
  );
  return sumRates(list, 'damage_reduce');
}
```

把与 `damageBoosts` 同行的 `sumRates(t.statuses, 'damage_reduce')` 换成 `sumReduce(t, hit)`（同一 `hit`）。

`pushStatus` 里 `damage_boost` / `damage_reduce` 把 `damageSource` / `skillTypes` / `damageType` 拷到 Status（与 `charges` 同样用 `in` 检查）。

- [ ] **Step 4: 跑测**

Run: `npx vitest run tests/troop_filter.test.ts tests/conflict.test.ts tests/damage_modifiers.test.ts`

Expected: PASS。`npx tsc --noEmit` PASS。

跳过 Commit。

---

### Task 4: 分兵 charges、奇数回合、recipient 代打

**Files:**
- Modify: `src/engine/action.ts`（`tickStatusesOnActStart`、`tickStatuses`、`pushStatus` 的 split、`tickRoundStartStatuses`、`executeSkillOutputs` physical_damage、`actUnit` 分兵段）
- Modify: `tests/troop_filter.test.ts`

**Interfaces:**
- Consumes: `split.charges`；`roundStartRepeat.oddRounds`；`attacker:'recipient'`
- Produces: 代打循环；`consumeSplitCharges`

- [ ] **Step 1: 写失败测试**

```ts
describe('oddRounds', () => {
  it('仅第 1/3 回合 roundStartRepeat 结算（maxRounds 3 时第 2 回合不挂分兵）', () => {
    const skill: Skill = {
      id: 'test_hy', name: '鹤翼', type: 'command', phase: 'prep', range: 3, triggerRate: 1,
      targetMode: 'group', groupCount: 3, targetSide: 'ally', tags: ['split'],
      output: [],
      roundStartRepeat: {
        oddRounds: true,
        output: [{ kind: 'inflict_status', troopTypes: ['archer'], status: { type: 'split', rate: 49, duration: 1 } }],
      },
    };
    const archer = dummyUnit('ar', '前锋', { troopType: 'archer', commandSkillIds: ['test_hy'], speed: 10 });
    const foe = dummyUnit('foe', '前锋', { speed: 200 }, 'enemy');
    const ctx = makeCtx([archer], [foe]);
    ctx.skills.set('test_hy', skill);
    triggerCommandSkills(ctx, archer);
    ctx.currentRound = 1;
    tickRoundStartStatuses(ctx);
    expect(archer.statuses.some((s) => s.type === 'split')).toBe(true);
    archer.statuses = [];
    ctx.currentRound = 2;
    tickRoundStartStatuses(ctx);
    expect(archer.statuses.some((s) => s.type === 'split')).toBe(false);
    ctx.currentRound = 3;
    tickRoundStartStatuses(ctx);
    expect(archer.statuses.some((s) => s.type === 'split')).toBe(true);
  });
});

describe('split charges', () => {
  it('有 charges 时 tickStatusesOnActStart 不因回合摘掉', () => {
    const me = dummyUnit('me', '前锋');
    const ctx = makeCtx([me], [dummyUnit('foe', '前锋', {}, 'enemy')]);
    ctx.currentRound = 2;
    me.statuses.push({
      type: 'split', remaining: 999, rate: 55, charges: 2, appliedRound: 1,
      sourceSkillType: 'active', sourceSkillId: 'sata_ruxing',
    });
    actUnit(ctx, me);
    const sp = me.statuses.find((s) => s.type === 'split');
    expect(sp).toBeTruthy();
    expect(sp && 'charges' in sp ? sp.charges : 0).toBeGreaterThanOrEqual(1);
  });
});

describe('attacker recipient', () => {
  it('骑兵代打：伤害 sourceId 为骑兵且 chance=1 必有 damage', () => {
    const skill: Skill = {
      id: 'test_ss2', name: '疏数打', type: 'command', phase: 'prep', range: 2, triggerRate: 1,
      targetMode: 'group', groupCount: 3, targetSide: 'ally', tags: ['damage'],
      teamTroopFilter: ['archer', 'cavalry'],
      output: [],
      roundStartRepeat: {
        output: [{
          kind: 'physical_damage',
          rate: 100,
          attacker: 'recipient',
          troopTypes: ['cavalry'],
          chance: 1,
          range: 3,
          targetMode: 'single',
          targetSide: 'enemy',
        }],
      },
    };
    const archer = dummyUnit('ar', '前锋', { troopType: 'archer', commandSkillIds: ['test_ss2'] });
    const cav = dummyUnit('cav', '中军', { troopType: 'cavalry', attack: 200 });
    const foe = dummyUnit('foe', '前锋', { defense: 40 }, 'enemy');
    const ctx = makeCtx([archer, cav], [foe]);
    ctx.skills.set('test_ss2', skill);
    triggerCommandSkills(ctx, archer);
    ctx.currentRound = 1;
    tickRoundStartStatuses(ctx);
    const dmg = ctx.events.filter((e) => e.type === 'damage' && e.sourceId === 'cav');
    expect(dmg.length).toBeGreaterThan(0);
  });
});
```

recipient 输出若没有 `targetSide`，`executeSkillOutputs` 的 `pool` 仍是锁定友军。代打循环应对 **pool 里每个骑兵** 另选敌军，不要把友军当伤害目标。实现见 Step 3。

- [ ] **Step 2: 跑测确认失败**

Run: `npx vitest run tests/troop_filter.test.ts`

Expected: FAIL（oddRounds 未实现或 recipient 未实现）。

- [ ] **Step 3: 实现**

`tickRoundStartStatuses` 窗口判断后加：

```ts
    if (rs.oddRounds && ctx.currentRound % 2 === 0) continue;
```

`tickStatusesOnActStart` 在 damage_boost charges 旁：

```ts
    if (s.type === 'split' && 'charges' in s && s.charges != null) continue;
```

`tickStatuses`：`appliedRound === 0` 的 split 若有 charges 也 `continue`。

`pushStatus` 的 split 分支拷贝 `charges`（有则写入）。

`actUnit` 分兵段：`executeSplitAttack` 每调用一次（每个 hitTarget）后：

```ts
function consumeSplitCharges(unit: UnitState): void {
  for (const s of [...unit.statuses]) {
    if (s.type !== 'split' || !('charges' in s) || s.charges == null) continue;
    s.charges -= 1;
    if (s.charges <= 0) unit.statuses = unit.statuses.filter((x) => x !== s);
  }
}
```

无 charges 的鹤翼分兵不调用此函数。

`executeSkillOutputs` `case 'physical_damage'`：若 `out.attacker === 'recipient'`：

1. 顶层 `chance`（先声夺人那种整段一次）对 recipient **跳过**（改在每人上 roll）。
2. `const enemies = ...` 已有。`const atkRange = out.range ?? skill.range`。
3. `for (const rider of pool)`：若 `chance` 则逐人 `moraleTriggerRate` + `skill_trigger`（targetId=rider）。失败 continue。
4. `skillTargets(ctx, rider, enemies, atkRange, 'single')` 取敌军。
5. 用 `rider` 当 `source` 走现有 physical 结算（`applyDamage(..., 'physical', 'skill')`，`damageBoosts(..., { damageSource:'skill', damageType:'physical', skillType: skill.type })`）。不要 `creditToId` 改成施法者。

`physical_damage` 普通路径：`attacker === 'lowest_strategy_ally'` 逻辑保持。

- [ ] **Step 4: 跑测**

Run: `npx vitest run tests/troop_filter.test.ts tests/main_skills_b16.test.ts tests/universal_b_plus_p1.test.ts`

Expected: PASS（运筹决胜 / 先声夺人不被 recipient 改坏）。`npx tsc --noEmit` PASS。

跳过 Commit。

---

### Task 5: 9 个战法入库 + SKILL_ID_BY_NAME

**Files:**
- Modify: `src/data/skills.ts`（`xiansheng_duoren` 块之后）
- Modify: `scripts/build_heroes_seed.mjs`（`先声夺人` 行后）

**Interfaces:**
- Consumes: Task 1–4 字段
- Produces: `fangyuan` `shushu` `henge` `fengshi` `yulin` `heyi` `bairen` `quanjun_tuji` `sata_ruxing`

- [ ] **Step 1: 在 `SKILL_REGISTRY` 追加（整段粘贴）**

块注释：`拆解通用 B+ 第二阶段兵种阵型`。

```ts
  fangyuan: {
    id: 'fangyuan',
    name: '方圆',
    type: 'command',
    phase: 'prep',
    range: 3,
    triggerRate: 1,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['damage_boost'],
    output: [
      {
        kind: 'inflict_status',
        troopTypes: ['infantry'],
        status: { type: 'damage_boost', rate: -0.2, duration: 999, direction: 'caused', damageSource: 'basic' },
      },
      {
        kind: 'inflict_status',
        troopTypes: ['infantry'],
        status: {
          type: 'damage_boost',
          rate: 0.168,
          duration: 999,
          direction: 'caused',
          damageSource: 'skill',
          skillTypes: ['active', 'pursuit'],
          defenseScaled: true,
        },
      },
    ],
  },
  shushu: {
    id: 'shushu',
    name: '疏数',
    type: 'command',
    phase: 'prep',
    range: 2,
    triggerRate: 1,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['buff_defense', 'damage'],
    teamTroopFilter: ['archer', 'cavalry'],
    output: [
      {
        kind: 'inflict_status',
        troopTypes: ['archer'],
        status: { type: 'defense_buff', amount: 50, duration: 999 },
      },
    ],
    roundStartRepeat: {
      output: [
        {
          kind: 'physical_damage',
          rate: 100,
          attacker: 'recipient',
          troopTypes: ['cavalry'],
          chance: 0.4,
          range: 3,
          targetMode: 'single',
        },
      ],
    },
  },
  henge: {
    id: 'henge',
    name: '衡轭',
    type: 'command',
    phase: 'prep',
    range: 2,
    triggerRate: 1,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['buff_strategy', 'damage_boost'],
    teamTroopFilter: ['cavalry', 'infantry'],
    output: [
      {
        kind: 'inflict_status',
        troopTypes: ['cavalry'],
        status: { type: 'strategy_buff', amount: 50, duration: 999 },
      },
      {
        kind: 'inflict_status',
        troopTypes: ['infantry'],
        status: { type: 'damage_boost', rate: 0.5, duration: 999, direction: 'caused', damageSource: 'basic' },
      },
    ],
  },
  fengshi: {
    id: 'fengshi',
    name: '锋矢',
    type: 'command',
    phase: 'prep',
    range: 3,
    triggerRate: 1,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['damage_boost'],
    output: [
      {
        kind: 'inflict_status',
        troopTypes: ['cavalry'],
        status: { type: 'damage_boost', rate: -0.25, duration: 999, direction: 'caused', damageSource: 'basic' },
      },
      {
        kind: 'inflict_status',
        troopTypes: ['cavalry'],
        status: {
          type: 'damage_boost',
          rate: 0.18,
          duration: 999,
          direction: 'caused',
          damageSource: 'skill',
          skillTypes: ['active'],
          speedScaled: true,
        },
      },
    ],
  },
  yulin: {
    id: 'yulin',
    name: '鱼鳞',
    type: 'command',
    phase: 'prep',
    range: 2,
    triggerRate: 1,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['buff_defense', 'damage_boost'],
    teamTroopFilter: ['infantry', 'archer'],
    output: [
      {
        kind: 'inflict_status',
        troopTypes: ['infantry'],
        status: { type: 'defense_buff', amount: 50, duration: 999 },
      },
      {
        kind: 'inflict_status',
        troopTypes: ['archer'],
        status: { type: 'damage_boost', rate: -0.35, duration: 999, direction: 'taken', damageType: 'strategy' },
      },
    ],
  },
  heyi: {
    id: 'heyi',
    name: '鹤翼',
    type: 'command',
    phase: 'prep',
    range: 3,
    triggerRate: 1,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['split', 'damage_boost'],
    output: [],
    roundStartRepeat: {
      oddRounds: true,
      output: [
        {
          kind: 'inflict_status',
          troopTypes: ['archer'],
          status: { type: 'split', rate: 49, duration: 1, strategyScaled: true },
        },
        {
          kind: 'inflict_status',
          troopTypes: ['archer'],
          status: { type: 'damage_boost', rate: -0.2, duration: 1, direction: 'caused', damageSource: 'basic' },
        },
      ],
    },
  },
  bairen: {
    id: 'bairen',
    name: '白刃',
    type: 'command',
    phase: 'prep',
    range: 5,
    triggerRate: 1,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['damage_boost', 'buff_defense', 'buff_attack'],
    output: [
      {
        kind: 'inflict_status',
        targetSide: 'ally',
        targetMode: 'all',
        status: { type: 'damage_boost', rate: -0.35, duration: 3, direction: 'caused', damageType: 'strategy' },
      },
      {
        kind: 'inflict_status',
        targetSide: 'enemy',
        targetMode: 'all',
        status: { type: 'damage_boost', rate: -0.35, duration: 3, direction: 'caused', damageType: 'strategy' },
      },
      {
        kind: 'inflict_status',
        troopTypes: ['cavalry', 'infantry'],
        status: { type: 'defense_buff', amount: 45, duration: 3 },
      },
    ],
    roundStartRepeat: {
      startRound: 4,
      endRound: 4,
      output: [
        {
          kind: 'inflict_status',
          troopTypes: ['cavalry', 'infantry'],
          status: { type: 'attack_buff', amount: 45, duration: 3 },
        },
      ],
    },
  },
  quanjun_tuji: {
    id: 'quanjun_tuji',
    name: '全军突击',
    type: 'active',
    prepare: false,
    range: 4,
    triggerRate: 0.35,
    targetMode: 'group',
    groupCount: 3,
    targetSide: 'ally',
    tags: ['immunity', 'damage', 'damage_boost'],
    output: [
      { kind: 'remove_debuffs', troopTypes: ['cavalry', 'infantry'] },
      { kind: 'physical_damage', rate: 145, targetSide: 'enemy', targetMode: 'single' },
      {
        kind: 'inflict_status',
        troopTypes: ['cavalry', 'infantry'],
        status: { type: 'damage_boost', rate: 0.28, duration: 999, direction: 'caused', charges: 2, strategyScaled: true },
      },
    ],
  },
  sata_ruxing: {
    id: 'sata_ruxing',
    name: '飒沓如星',
    type: 'active',
    prepare: false,
    range: 2,
    triggerRate: 0.4,
    targetMode: 'group',
    groupCount: 2,
    targetSide: 'ally',
    tags: ['damage_boost', 'split'],
    output: [
      {
        kind: 'inflict_status',
        troopTypes: ['cavalry'],
        status: {
          type: 'damage_boost',
          rate: 0.36,
          duration: 2,
          direction: 'caused',
          damageSource: 'basic',
          strategyScaled: true,
        },
      },
      {
        kind: 'inflict_status',
        troopTypes: ['cavalry'],
        status: { type: 'split', rate: 55, duration: 999, charges: 2 },
      },
    ],
  },
```

每条加 JSDoc 中文（满级 extra 摘要 + 基值无成长率）。

`scripts/build_heroes_seed.mjs` 在 `先声夺人: 'xiansheng_duoren',` 后：

```js
  方圆: 'fangyuan',
  疏数: 'shushu',
  衡轭: 'henge',
  锋矢: 'fengshi',
  鱼鳞: 'yulin',
  鹤翼: 'heyi',
  白刃: 'bairen',
  全军突击: 'quanjun_tuji',
  飒沓如星: 'sata_ruxing',
```

不跑 `build_heroes_seed.mjs`。

- [ ] **Step 2: `npx tsc --noEmit`**

Expected: PASS。

- [ ] **Step 3: smoke**

Run: `npx vitest run tests/troop_filter.test.ts`

Expected: PASS。

跳过 Commit。

---

### Task 6: 战法测试 + classified + 清点 + gen_skill_data

**Files:**
- Create: `tests/universal_b_plus_p2.test.ts`
- Modify: `scripts/_classified.json`（方圆、疏数、衡轭、锋矢、鱼鳞、鹤翼、白刃、全军突击、飒沓如星 的 `status` → `implemented`，清空 `missingMechanics`）
- Modify: `docs/待补充机制清点.md`（文首 69→60；删 troop_type 那行的 7 个或整行若已空；删 troop_next 行）
- Modify: `web/data/skill_grades.json` / `skill_desc.json`（跑生成脚本，不要手改）

**Interfaces:**
- Consumes: `SKILL_REGISTRY` 九 id
- Produces: 每战法 3 测；dataIntegrity 覆盖新 id

- [ ] **Step 1: 写 `tests/universal_b_plus_p2.test.ts`**

木桩对齐 `tests/universal_b_plus_p1.test.ts`（完整拷贝 `dummy` / `enemyTeam` / `run` / `commandTeam` / `commandFront` / `asCommand`）。另加 `activeTeam(skillId)`：前锋 `activeSkillIds:[skillId]`，`morale: 140` 便于主动发动。骑兵木桩：`troopType:'cavalry'`。弓兵：`archer`。

每战法 3 测（装配 / 机制 / 窗口或兵种）：

**方圆：** ① `asCommand` range 3 groupCount 3、两条 inflict troopTypes infantry。② 三步兵队伍 `run` 后步兵有 `damageSource:'basic'` 的 caused −0.2。③ 步兵 `activeSkillIds` 挂一个已有主动物理（若不想依赖发动率，机制测用 Task 2 同款 `triggerCommandSkills` + 断言状态字段 `skillTypes` 含 `pursuit`）。

**疏数：** ① teamTroopFilter 弓+骑。② 弓+骑+步 `run` 无 skill_cast 疏数。③ 弓+骑队伍弓兵 defense_buff 50。

**衡轭：** ① teamTroopFilter 骑+步。② 骑兵 strategy_buff 50。③ 步兵 caused basic +0.5。

**锋矢：** ① 骑兵 basic −0.25。② 第二条 skillTypes 仅 `['active']`。③ 步兵队伍挂锋矢，步兵无这些 boost。

**鱼鳞：** ① teamTroopFilter 步+弓。② 步兵防御 +50。③ 弓兵 taken −0.35 `damageType:'strategy'`。

**鹤翼：** ① oddRounds true、output 空。② `run` maxRounds 2：第 1 回合弓兵有 split，用事件 round 窗口断言第 2 回合 split 已尽（duration 1）。③ 骑兵挂鹤翼无 split。

**白刃：** ① 两段 targetSide ally/enemy all 策略 −0.35 duration 3。② 骑/步 defense 45。③ `run` 到第 4 回合骑/步出现 attack_buff 45。

**全军突击：** ① active 0.35 range 4 group 3 ally。② output 三段 kind。③ 骑兵 charges:2 caused 0.28。

**飒沓如星：** ① groupCount 2。② 骑兵 basic +0.36 duration 2。③ split charges 2 rate 55。

主动战法装配测不依赖发动成功。

- [ ] **Step 2: 跑本批测试**

Run: `npx vitest run tests/universal_b_plus_p2.test.ts tests/troop_filter.test.ts`

Expected: PASS。失败则修战法字段或引擎，不改断言去就数据。

- [ ] **Step 3: classified + 清点 + gen_skill_data**

九个名字：`status: "implemented"`，`missingMechanics: ""`，`note: "troop filter p2"`。

清点：当前 **69** 改为 **60**。机制表删除方圆…白刃（troop_type 若只剩空则删整行）。`troop_next` 行删除。引擎现状段补一句：兵种过滤 / 普攻主动分流已入库。

Run: `node scripts/gen_skill_data.mjs`

Expected: `skill_grades` / `skill_desc` 含九 id。

- [ ] **Step 4: 全量回归**

Run: `npx tsc --noEmit`  
Expected: PASS

Run: `npx vitest run`  
Expected: 全绿，0 failed。golden 2/2 且 **不要** 删 `tests/__snapshots__/golden.json`。若 golden 失败：先查是否误伤 T1～T9；仅当确认是本批必然副作用且用户同意才重生。默认修引擎而不是重生。

跳过 Commit。

---

## Self-Review

1. **Spec coverage:** 9 战法 Task 5–6；`teamTroopFilter` Task 2；`troopTypes` Task 2；增减伤维度 Task 3；split charges / oddRounds / recipient Task 4；白刃两段 Task 5；classified/清点/gen_skill_data Task 6。分兵 applyDamage 仍为 skill、增减伤当 basic：Global Constraints。
2. **Placeholders:** 无 TBD。Task 3 明确不导出 `damageBoosts`、用 `statusMatchesHit`。
3. **Types:** `recipient` / `oddRounds` / `DamageHitContext` 前后一致。`remove_debuffs` 改为带可选字段的对象。

# 受击 / 受恢复钩子（于禁补全 · 贾充录入）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 扩展 `onHurt`（伤害类型过滤、钩子自带 output、防御缩放）并新增 `onHeal`，补全于禁当敌制决反制、完整录入贾充赏顺伐逆。

**Architecture:** 不抽事件总线。`applyDamage` 带上 `damageType` 后走现有 `triggerOnHurt`；`recoverTroops` 实际回血后再走对称的 `triggerOnHeal`。钩子 `output` 交给已有 `executeSkillOutputs(ctx, caster, skill, targets, outputs?)`。

**Tech Stack:** TypeScript 引擎、Vitest、`SKILL_REGISTRY` / `SKILL_ID_BY_NAME` / `web/data/heroes.json`。

## Global Constraints

- Spec：`docs/superpowers/specs/2026-09-15-受击受恢复钩子-于禁贾充-design.md`
- 禁止 `Math.random` 进入引擎；随机只走 `rng.ts`
- JSDoc 用中文
- 贾充恢复谋略成长 **0.325**（已确认）；反击 180% 仍未知：**取基值**（`strategyScaled: false`、`growthRate: 0`），不编造反击成长
- 于禁反制成长率 **0.026/点（防御）**；减伤 50% 不改
- 本批不做周泰 / 羊祜 / 卫子夫 / 陈到 / 张梁 / 孙坚
- **不要 git commit**（除非用户在本会话明确要求提交）；跳过所有 Commit 步骤

## File Structure

| 文件 | 职责 |
|------|------|
| `src/engine/types.ts` | `OnHurtConfig` 增量、`OnHealConfig`、`damage_boost.defenseScaled`、指挥/被动挂 `onHeal` |
| `src/engine/action.ts` | `CombatContext.resolvingHealHooks`；`applyDamage` 传类型；`triggerOnHurt` 过滤；`triggerOnHeal`；防御缩放 |
| `src/data/skills.ts` | `dangdi_zhijue` 补全；新增 `shangshun_fani` |
| `scripts/build_heroes_seed.mjs` | `赏顺伐逆: 'shangshun_fani'` |
| `web/data/heroes.json` | `h708.mainSkillId = "shangshun_fani"`（`initHeroDB` 无库时的回退源） |
| `scripts/seed_heroes.sql` | 跑 `build_heroes_seed.mjs` 重生，或只改贾充那一行 |
| `tests/main_skills_b2.test.ts` | 于禁反制追加 |
| `tests/main_skills_b19.test.ts` | 贾充（下一批号；仓库已有 b18） |

---

### Task 1: 类型（OnHurt / OnHeal / defenseScaled）

**Files:**
- Modify: `src/engine/types.ts`（`CreateStatus` 的 `damage_boost`、`OnHurtConfig`、`CommandSkill`、`PassiveSkill`）
- Modify: `src/engine/action.ts`（仅 `CombatContext` 加 `resolvingHealHooks?: boolean`）

**Interfaces:**
- Consumes: 现有 `SkillOutput`、`OnHurtConfig`、`CreateStatus`
- Produces: `OnHealConfig`；`OnHurtConfig.damageKind` / `OnHurtConfig.output`；`CommandSkill.onHeal` / `PassiveSkill.onHeal`；`damage_boost.defenseScaled`

- [ ] **Step 1: 改 `CreateStatus` 的 `damage_boost`**

在 `src/engine/types.ts` 的 `damage_boost` 变体上，`strategyScaled?: boolean` 旁增加：

```ts
  | { type: 'damage_boost'; rate: number; duration: number; direction?: 'caused' | 'taken'; stacks?: number; strategyScaled?: boolean; /** 受防御缩放（当敌制决 +8%，公式同受谋略，属性换生效防御） */ defenseScaled?: boolean; growthRate?: number; charges?: number; chargesStack?: boolean }
```

保留该行原有 JSDoc（direction / stacks / strategyScaled / charges / chargesStack），把 `defenseScaled` 写进同一条注释。

- [ ] **Step 2: 扩展 `OnHurtConfig` 并新增 `OnHealConfig`**

`OnHurtConfig` 在 `steal?` 之后追加：

```ts
  /** 只匹配该类伤害；缺省两类都吃。赏顺伐逆只吃策略（含 DoT） */
  damageKind?: 'physical' | 'strategy';
  /** 缺省 `skill.output`（典韦/盲侯）。于禁反制 / 贾充反击自带这段，不覆盖准备阶段 output */
  output?: SkillOutput[];
```

紧接 `OnHurtConfig` 之后插入：

```ts
/**
 * 受恢复触发（赏顺伐逆）：`recoverTroops` 实际恢复 > 0 后判定。
 * 处理期间 `resolvingHealHooks` 防重入，避免自己奶自己再套一层。
 */
export interface OnHealConfig {
  /** 谁被恢复时判定：self=施法者自身 / ally=同侧含自己 */
  victim: 'self' | 'ally';
  /** 基础触发率 0~1，缺省 1。赏顺伐逆 0.75 */
  rate?: number;
  /**
   * 效果落点：
   * - allies：友军全体（含自己）结算 output（赏顺伐逆群体恢复）
   * - self：只对施法者结算
   */
  applyTo: 'allies' | 'self';
  /** 缺省 `skill.output` */
  output?: SkillOutput[];
}
```

- [ ] **Step 3: 指挥 / 被动挂 `onHeal`**

`CommandSkill` 的 `onHurt?: OnHurtConfig` 旁增加 `onHeal?: OnHealConfig`。

`PassiveSkill` 同样：`onHurt` 旁加 `onHeal?: OnHealConfig`。

- [ ] **Step 4: `CombatContext` 加防重入标志**

`src/engine/action.ts` 的 `CombatContext`，在 `resolvingHurtHooks?: boolean` 旁：

```ts
  /** 受恢复 hook 重入保护：赏顺伐逆群体奶不再触发 onHeal */
  resolvingHealHooks?: boolean;
```

- [ ] **Step 5: 类型检查**

Run: `npx tsc --noEmit`

Expected: PASS（只加可选字段，现有战法不用改）

- [ ] **Step 6: Commit**

跳过（Global Constraints）。

---

### Task 2: `applyDamage` 传伤害类型 + `onHurt` 过滤 / 钩子 output / 防御缩放

**Files:**
- Modify: `src/engine/action.ts`（`applyDamage`、`triggerOnHurt`、`applyOnHurtEffect`、所有 `applyDamage(` 调用点、`executeSkillOutputs` 的 `damage_boost` 缩放分支）
- Test: `tests/main_skills_b2.test.ts`（先写于禁反制失败用例，本任务实现到能过防御 80→8%）

**Interfaces:**
- Consumes: Task 1 的 `damageKind` / `output` / `defenseScaled`
- Produces: `applyDamage(ctx, target, damage, source?, damageType?: DamageType): void`；`triggerOnHurt` 识别 `damageKind`；`applyOnHurtEffect` 把 `cfg.output` 传入 `executeSkillOutputs` 第 5 参

- [ ] **Step 1: 写失败测试（于禁反制，防御钉 80 → taken 8%）**

在 `tests/main_skills_b2.test.ts` 增加 action 侧 import 与 ctx 辅助（与 `tests/main_skills_b14.test.ts` 同构），放在文件顶部现有 import 之后：

```ts
import { applyDamage, triggerCommandSkills, type CombatContext } from '../src/engine/action';
import type { Skill, UnitState } from '../src/engine/types';
import { Rng } from '../src/engine/rng';
```

在现有 `当敌制决` describe **之外**、`闭月` describe **之前**，追加辅助（若与 b14 重复，可只在本文件内联一份）：

```ts
function dummyUnit(id: string, position: Position, extra: Partial<General> = {}, side: 'my' | 'enemy' = 'my'): UnitState {
  const g: General = {
    id,
    name: id,
    rarity: '4星',
    cost: 1,
    faction: '汉',
    tags: [],
    mutualExclusionGroup: null,
    troopType: 'infantry',
    position,
    attack: 80,
    defense: 80,
    strategy: 80,
    speed: 50,
    attackRange: 2,
    maxTroops: 10000,
    mainSkillName: '',
    skillDesc: '',
    activeSkillIds: [],
    passiveSkillIds: [],
    commandSkillIds: [],
    pursuitSkillIds: [],
    morale: 100,
    ...extra,
  };
  return {
    general: g,
    side,
    troops: g.maxTroops,
    wounded: 0,
    totalDead: 0,
    alive: true,
    statuses: [],
    isPreparing: false,
    preparingSkillId: null,
    hasActedThisRound: false,
  };
}

function makeCtx(my: UnitState[], enemy: UnitState[], seed = 1): CombatContext {
  return {
    rng: new Rng(seed),
    myTeam: my,
    enemyTeam: enemy,
    events: [],
    skills: new Map<string, Skill>(Object.entries(SKILL_REGISTRY)),
    lockedCommands: [],
    stackBuffs: [],
    currentRound: 1,
  };
}
```

在原「于禁受到的普攻伤害显著低于无减伤对照」那条 `it` 之后追加：

```ts
  it('受击后使伤害来源受到的伤害提升 8%（防御 80 取基值），可叠加', () => {
    const yujin = dummyUnit('h796', '前锋', { defense: 80, commandSkillIds: ['dangdi_zhijue'] });
    const foe = dummyUnit('foe', '前锋', {}, 'enemy');
    const ctx = makeCtx([yujin], [foe]);
    triggerCommandSkills(ctx, yujin);
    applyDamage(ctx, yujin, 100, foe, 'physical');
    const boost = foe.statuses.find(
      (s): s is Extract<(typeof foe.statuses)[number], { type: 'damage_boost' }> =>
        s.type === 'damage_boost' && s.sourceSkillId === 'dangdi_zhijue'
    );
    expect(boost).toBeDefined();
    expect(boost!.direction ?? 'taken').toBe('taken');
    expect(boost!.rate).toBeCloseTo(0.08, 5);
    applyDamage(ctx, yujin, 100, foe, 'physical');
    expect(boost!.rate).toBeCloseTo(0.16, 5);
  });
```

原减伤 4 条不要改。此条此时应失败：来源身上没有 `damage_boost`。

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/main_skills_b2.test.ts -t "受击后使伤害来源"`

Expected: FAIL（找不到 `damage_boost` 或 `onHurt` 未定义）

- [ ] **Step 3: 改 `applyDamage` 签名并下传类型**

把

```ts
export function applyDamage(ctx: CombatContext, target: UnitState, damage: number, source?: UnitState): void {
```

改为：

```ts
export function applyDamage(
  ctx: CombatContext,
  target: UnitState,
  damage: number,
  source?: UnitState,
  damageType?: DamageType
): void {
```

文件顶部若未从 `./types` 导入 `DamageType`，补上。

将末尾 `if (actual > 0) triggerOnHurt(ctx, target, source);` 改为 `if (actual > 0) triggerOnHurt(ctx, target, source, damageType);`。

`triggerOnHurt` 签名改为 `(ctx, victim, source?, damageType?: DamageType)`。在 `matchOnHurtVictim` 通过之后、`source` 距离检查之前插入：

```ts
        if (cfg.damageKind && damageType && cfg.damageKind !== damageType) continue;
```

（`damageType` 缺省不传 → 不过滤，旧单元测试保持原行为。）

- [ ] **Step 4: 全部引擎调用点传入 `damageType`**

只改 `src/engine/action.ts` 里现有 `applyDamage(`：

| 行附近 | 调用 | 第 5 参 |
|--------|------|---------|
| delayedOutput 预存伤害 | `applyDamage(ctx, target, d.damage, caster)` | `'strategy'` |
| `positional_physical_damage` | `applyDamage(ctx, t, capped, source)` | `'physical'` |
| `dealDotDamage` 两处 | `applyDamage(ctx, unit, capped, src)` | `'strategy'` |
| 分兵 | `applyDamage(ctx, adjTarget, capped, unit)` | `'physical'` |
| `physical_damage` | `applyDamage(ctx, t, capped, source)` | `'physical'` |
| `strategy_damage` | `applyDamage(ctx, t, capped, caster)` | `'strategy'` |
| 普攻 `normalAttack` | `applyDamage(ctx, hit, capped, unit)` | `'physical'` |

- [ ] **Step 5: `applyOnHurtEffect` 使用钩子 `output`**

四处 `executeSkillOutputs(ctx, caster, skill, …)` 都加第 5 参 `cfg.output`：

```ts
    executeSkillOutputs(ctx, caster, skill, targets, cfg.output);
    // source / victim / allies 三支同样加 cfg.output
```

`steal` 分支不走 output，不动。

- [ ] **Step 6: 防御缩放（与 `strategyScaled` 并列）**

在 `executeSkillOutputs` 的 `damage_boost && create.strategyScaled` 分支**之后**插入：

```ts
          } else if (create.type === 'damage_boost' && create.defenseScaled && create.growthRate !== undefined) {
            /** 增减伤受防御影响（当敌制决 +8%，成长 0.026/点）：公式同受谋略，属性换生效防御 */
            const sign = Math.sign(create.rate) || 1;
            const scaled =
              (roundRate(scaledValue(Math.abs(create.rate) * 100, create.growthRate, effectiveStat(caster, 'defense'))) /
                100) *
              sign;
            inflictStatus(ctx, t, { ...create, rate: scaled }, skill.type, skill.id, caster.general.id);
```

- [ ] **Step 7: 补全 `dangdi_zhijue`（本任务先挂 onHurt，测试才能绿）**

`src/data/skills.ts` 替换现有 `dangdi_zhijue`：

```ts
  /**
   * 当敌制决（于禁主战法·二类指挥·战斗开始一次性）：
   * 自身受到伤害降低 50%；受到伤害后使伤害来源受到的伤害提升 8%（受防御，成长 0.026/点），可叠加至战斗结束。
   * 反制走 onHurt.applyTo:'source'；准备阶段只跑减伤（battleStartOnce）。
   */
  dangdi_zhijue: {
    id: 'dangdi_zhijue',
    name: '当敌制决',
    type: 'command',
    phase: 'round',
    roundTrigger: 'on_act',
    battleStartOnce: true,
    range: 5,
    triggerRate: 1,
    targetMode: 'self',
    tags: ['damage_reduce', 'damage_boost'],
    onHurt: {
      victim: 'self',
      applyTo: 'source',
      output: [
        {
          kind: 'inflict_status',
          status: {
            type: 'damage_boost',
            rate: 0.08,
            duration: 999,
            direction: 'taken',
            defenseScaled: true,
            growthRate: 0.026,
          },
        },
      ],
    },
    output: [{ kind: 'inflict_status', status: { type: 'damage_reduce', rate: 0.5, duration: 999 }, target: 'self' }],
  },
```

`battleStartOnce` 分支仍 `executeSkillOutputs(ctx, unit, skill, [unit])`（不带 onHurt.output），减伤不变。不要把 `onHurt` 加进「准备阶段跳过 output」那条一类指挥逻辑——于禁走的是 `battleStartOnce` 早退。

- [ ] **Step 8: 跑于禁新测试 + 原 4 条**

Run: `npx vitest run tests/main_skills_b2.test.ts`

Expected: PASS（闭月等其它 describe 也过）

- [ ] **Step 9: Commit**

跳过。

---

### Task 3: 于禁反制补充断言（DoT、防御 172、致死）

**Files:**
- Modify: `tests/main_skills_b2.test.ts`
- Test: 同上

**Interfaces:**
- Consumes: Task 2 的 `applyDamage(..., damageType)` 与 `dangdi_zhijue.onHurt`
- Produces: 无新 API

- [ ] **Step 1: 追加三条测试**

仍在 `当敌制决` describe 内：

```ts
  it('防御 172 时反制为 10%（8 + 0.026×92，八舍九入）', () => {
    const yujin = dummyUnit('h796', '前锋', { defense: 172, commandSkillIds: ['dangdi_zhijue'] });
    const foe = dummyUnit('foe', '前锋', {}, 'enemy');
    const ctx = makeCtx([yujin], [foe]);
    triggerCommandSkills(ctx, yujin);
    applyDamage(ctx, yujin, 100, foe, 'physical');
    const boost = foe.statuses.find(
      (s): s is Extract<(typeof foe.statuses)[number], { type: 'damage_boost' }> =>
        s.type === 'damage_boost' && s.sourceSkillId === 'dangdi_zhijue'
    );
    expect(boost?.rate).toBeCloseTo(0.1, 5);
  });

  it('策略 DoT 跳伤也叠反制', () => {
    const yujin = dummyUnit('h796', '前锋', { defense: 80, commandSkillIds: ['dangdi_zhijue'] });
    const foe = dummyUnit('foe', '前锋', {}, 'enemy');
    const ctx = makeCtx([yujin], [foe]);
    triggerCommandSkills(ctx, yujin);
    applyDamage(ctx, yujin, 80, foe, 'strategy');
    const boost = foe.statuses.find(
      (s) => s.type === 'damage_boost' && s.sourceSkillId === 'dangdi_zhijue'
    );
    expect(boost).toBeDefined();
  });

  it('致死那一下仍叠；阵亡后再 applyDamage 不再叠', () => {
    const yujin = dummyUnit('h796', '前锋', { defense: 80, commandSkillIds: ['dangdi_zhijue'] });
    yujin.troops = 50;
    const foe = dummyUnit('foe', '前锋', {}, 'enemy');
    const ctx = makeCtx([yujin], [foe]);
    triggerCommandSkills(ctx, yujin);
    applyDamage(ctx, yujin, 50, foe, 'physical');
    expect(yujin.alive).toBe(false);
    const boost = foe.statuses.find(
      (s): s is Extract<(typeof foe.statuses)[number], { type: 'damage_boost' }> =>
        s.type === 'damage_boost' && s.sourceSkillId === 'dangdi_zhijue'
    );
    expect(boost?.rate).toBeCloseTo(0.08, 5);
    applyDamage(ctx, yujin, 100, foe, 'physical');
    expect(boost?.rate).toBeCloseTo(0.08, 5);
  });
```

- [ ] **Step 2: 跑测试**

Run: `npx vitest run tests/main_skills_b2.test.ts -t "当敌制决"`

Expected: PASS

- [ ] **Step 3: 装配断言补 range / tags / onHurt**

原「主战法挂入指挥槽」那条增加：

```ts
    expect(s.range).toBe(5);
    expect(s.tags).toEqual(['damage_reduce', 'damage_boost']);
    expect(s.type === 'command' && s.onHurt?.applyTo).toBe('source');
```

（`s` 在 `type === 'command'` 收窄后再读 `onHurt`，与 b14 夏侯惇写法一致。）

- [ ] **Step 4: Commit**

跳过。

---

### Task 4: `onHeal` + `recoverTroops` 挂钩

**Files:**
- Modify: `src/engine/action.ts`（`recoverTroops`、新增 `triggerOnHeal` / `matchOnHealVictim` / `applyOnHealEffect` / `rollOnHeal`）
- Test: `tests/main_skills_b19.test.ts`（本任务先写强制 onHeal 的失败用例，战法定义在 Task 5 才进 registry；若 Task 4 与 5 同会话，可先 5 再回头跑本测试）

**Interfaces:**
- Consumes: Task 1 `OnHealConfig`、`resolvingHealHooks`
- Produces: `triggerOnHeal(ctx, target: UnitState): void`（模块内私有即可）；`recoverTroops` 在实际恢复 `> 0` 后调用它

实现说明：贾充战法在 Task 5 才写入 `SKILL_REGISTRY`。本任务可以**只实现引擎函数**，用 Task 5 的测试验收；若希望本任务单独可测，在测试里 `ctx.skills.set` 一份临时被动。推荐与 Task 5 连续做，本任务 Step 1 的测试放到 `b19`，Task 5 补 registry 后一起绿。

- [ ] **Step 1: 实现 `triggerOnHeal`**

放在 `recoverTroops` 上方。逻辑对齐 `triggerOnHurt`（扫描双方 `commandSkillIds` + `passiveSkillIds`）：

```ts
function matchOnHealVictim(cfg: OnHealConfig, caster: UnitState, target: UnitState): boolean {
  if (cfg.victim === 'self') return caster.general.id === target.general.id;
  return caster.side === target.side;
}

function rollOnHeal(
  ctx: CombatContext,
  caster: UnitState,
  cfg: OnHealConfig
): { success: boolean; rate: number; baseRate: number } {
  const base = cfg.rate ?? 1;
  const rate = moraleTriggerRate(effectiveMorale(caster), base);
  return { success: ctx.rng.chance(rate), rate, baseRate: base };
}

function applyOnHealEffect(
  ctx: CombatContext,
  caster: UnitState,
  skill: Skill,
  cfg: OnHealConfig
): void {
  ctx.events.push({
    type: 'skill_cast',
    unitId: caster.general.id,
    skillId: skill.id,
    skillName: skill.name,
  });
  if (cfg.applyTo === 'self') {
    executeSkillOutputs(ctx, caster, skill, [caster], cfg.output);
    return;
  }
  const team = caster.side === 'my' ? ctx.myTeam : ctx.enemyTeam;
  const allies = team.filter((a) => a.alive);
  executeSkillOutputs(ctx, caster, skill, allies, cfg.output);
}

function triggerOnHeal(ctx: CombatContext, target: UnitState): void {
  if (ctx.resolvingHealHooks) return;
  ctx.resolvingHealHooks = true;
  try {
    const all = ctx.myTeam.concat(ctx.enemyTeam);
    for (const caster of all) {
      const ids = [...caster.general.commandSkillIds, ...caster.general.passiveSkillIds];
      for (const id of ids) {
        const skill = resolveSkill(ctx, id);
        if (!skill || (skill.type !== 'command' && skill.type !== 'passive') || !skill.onHeal) continue;
        const cfg = skill.onHeal;
        if (!caster.alive && !skill.retainAfterDeath) continue;
        if (!matchOnHealVictim(cfg, caster, target)) continue;
        const rolled = rollOnHeal(ctx, caster, cfg);
        if ((cfg.rate ?? 1) < 1) {
          ctx.events.push({
            type: 'skill_trigger',
            unitId: caster.general.id,
            targetId: target.general.id,
            skillId: skill.id,
            skillName: skill.name,
            success: rolled.success,
            rate: Math.round(rolled.rate * 100),
            baseRate: Math.round(rolled.baseRate * 100),
            morale: effectiveMorale(caster),
          });
        }
        if (!rolled.success) continue;
        applyOnHealEffect(ctx, caster, skill, cfg);
      }
    }
  } finally {
    ctx.resolvingHealHooks = false;
  }
}
```

从 `../engine/types` 或文件现有 import 增加 `OnHealConfig`。

- [ ] **Step 2: `recoverTroops` 成功后调用**

`recoverTroops` 在 `return recoverable;` 之前：

```ts
  if (recoverable > 0) triggerOnHeal(ctx, target);
```

保持「已阵亡或兵力 0 返回 0」早退，那些路径不要调 `triggerOnHeal`。

- [ ] **Step 3: 类型检查**

Run: `npx tsc --noEmit`

Expected: PASS

- [ ] **Step 4: Commit**

跳过。

---

### Task 5: 赏顺伐逆数据 + 贾充测试 + 挂槽

**Files:**
- Modify: `src/data/skills.ts`（`SKILL_REGISTRY` 末尾合适位置，典韦块之后即可）
- Modify: `scripts/build_heroes_seed.mjs`（`SKILL_ID_BY_NAME`）
- Modify: `web/data/heroes.json`（`h708.mainSkillId`）
- Modify: 跑 `node scripts/build_heroes_seed.mjs` 更新 `scripts/seed_heroes.sql`（无库时至少改 json）
- Create: `tests/main_skills_b19.test.ts`

**Interfaces:**
- Consumes: Task 2 `damageKind` / 钩子 `output`；Task 4 `onHeal`
- Produces: `SKILL_REGISTRY.shangshun_fani`；`h708` 被动槽含该 id

- [ ] **Step 1: 写失败测试 `tests/main_skills_b19.test.ts`**

```ts
/**
 * 赏顺伐逆（贾充）：受恢复 75% 友军全体奶 65%（取基值）；受策略伤害 75% 对来源策略 180%（取基值）。
 */
import { describe, it, expect, beforeAll } from 'vitest';
import {
  applyDamage,
  recoverTroops,
  triggerPassiveSkills,
  type CombatContext,
} from '../src/engine/action';
import type { BattleEvent, General, PassiveSkill, Position, Skill, UnitState } from '../src/engine/types';
import { SKILL_REGISTRY } from '../src/data/skills';
import { initHeroDB, HERO_REGISTRY, withSkills } from '../src/data/heroes';
import { Rng } from '../src/engine/rng';

beforeAll(async () => {
  await initHeroDB();
});

function hero(id: string): General {
  return { ...HERO_REGISTRY[id] };
}

function dummy(id: string, position: Position, extra: Partial<General> = {}): General {
  return {
    id,
    name: id,
    rarity: '4星',
    cost: 1,
    faction: '晋',
    tags: [],
    mutualExclusionGroup: null,
    troopType: 'infantry',
    position,
    attack: 80,
    defense: 80,
    strategy: 80,
    speed: 50,
    attackRange: 5,
    maxTroops: 10000,
    mainSkillName: '',
    skillDesc: '',
    activeSkillIds: [],
    passiveSkillIds: [],
    commandSkillIds: [],
    pursuitSkillIds: [],
    morale: 100,
    ...extra,
  };
}

function makeUnit(g: General, side: 'my' | 'enemy' = 'my'): UnitState {
  return {
    general: g,
    side,
    troops: g.maxTroops,
    wounded: 0,
    totalDead: 0,
    alive: true,
    statuses: [],
    isPreparing: false,
    preparingSkillId: null,
    hasActedThisRound: false,
  };
}

function makeCtx(my: UnitState[], enemy: UnitState[], seed = 1): CombatContext {
  return {
    rng: new Rng(seed),
    myTeam: my,
    enemyTeam: enemy,
    events: [],
    skills: new Map<string, Skill>(Object.entries(SKILL_REGISTRY)),
    lockedCommands: [],
    stackBuffs: [],
    currentRound: 1,
  };
}

function forceOnHealRate(ctx: CombatContext, skillId: string, rate = 1): void {
  const base = ctx.skills.get(skillId);
  if (!base || (base.type !== 'command' && base.type !== 'passive') || !base.onHeal) {
    throw new Error(`forceOnHealRate：${skillId} 无 onHeal`);
  }
  ctx.skills.set(skillId, { ...base, onHeal: { ...base.onHeal, rate } } as Skill);
}

function forceOnHurtRate(ctx: CombatContext, skillId: string, rate = 1): void {
  const base = ctx.skills.get(skillId);
  if (!base || (base.type !== 'command' && base.type !== 'passive') || !base.onHurt) {
    throw new Error(`forceOnHurtRate：${skillId} 无 onHurt`);
  }
  ctx.skills.set(skillId, { ...base, onHurt: { ...base.onHurt, rate } } as Skill);
}

describe('赏顺伐逆（贾充，被动：受恢复奶全体 / 受策略反击来源）', () => {
  it('装配：贾充主战法挂入被动槽', async () => {
    const g = hero('h708');
    expect(g.name).toBe('贾充');
    expect(g.passiveSkillIds).toContain('shangshun_fani');
    const s = SKILL_REGISTRY['shangshun_fani'];
    expect(s.type).toBe('passive');
    if (s.type === 'passive') {
      expect(s.timing).toBe('battle_start');
      expect(s.range).toBe(5);
      expect(s.onHeal?.rate).toBe(0.75);
      expect(s.onHeal?.applyTo).toBe('allies');
      expect(s.onHurt?.damageKind).toBe('strategy');
      expect(s.onHurt?.applyTo).toBe('source');
      expect(s.onHurt?.sourceMaxDistance).toBe(5);
    }
  });

  it('受恢复后 75% 为友军全体恢复（取基值 65%）；自身群体奶不重入', () => {
    const jc = makeUnit(withSkills(dummy('h708', '中军', { strategy: 200 }), { passiveSkillIds: ['shangshun_fani'] }));
    const ally = makeUnit(dummy('ally', '前锋'));
    const foe = makeUnit(dummy('foe', '前锋'), 'enemy');
    jc.troops = 5000;
    ally.troops = 5000;
    const ctx = makeCtx([jc, ally], [foe]);
    triggerPassiveSkills(ctx, jc);
    forceOnHealRate(ctx, 'shangshun_fani', 1);
    recoverTroops(ctx, jc, 100);
    const heals = ctx.events.filter((e): e is Extract<BattleEvent, { type: 'heal' }> => e.type === 'heal');
    expect(heals.length).toBe(2);
    expect(heals.map((e) => e.targetId).sort()).toEqual(['ally', 'h708']);
    const casts = ctx.events.filter((e) => e.type === 'skill_cast' && e.skillId === 'shangshun_fani');
    expect(casts.length).toBe(1);
  });

  it('受策略伤害反击来源 180%；普攻不反击；DoT 算策略', () => {
    const jc = makeUnit(withSkills(dummy('h708', '中军'), { passiveSkillIds: ['shangshun_fani'] }));
    const foe = makeUnit(dummy('foe', '前锋', { maxTroops: 20000 }), 'enemy');
    foe.troops = 20000;
    const ctx = makeCtx([jc], [foe]);
    triggerPassiveSkills(ctx, jc);
    forceOnHurtRate(ctx, 'shangshun_fani', 1);
    const hpAfterPhys = foe.troops;
    applyDamage(ctx, jc, 50, foe, 'physical');
    expect(foe.troops).toBe(hpAfterPhys);
    applyDamage(ctx, jc, 50, foe, 'strategy');
    expect(foe.troops).toBeLessThan(hpAfterPhys);
    const dmg = ctx.events.filter(
      (e) => e.type === 'damage' && e.skillId === 'shangshun_fani' && e.damageType === 'strategy'
    );
    expect(dmg.length).toBeGreaterThanOrEqual(1);
  });
});
```

Run: `npx vitest run tests/main_skills_b19.test.ts`

Expected: FAIL（registry / 挂槽缺失）

- [ ] **Step 2: 写入 `shangshun_fani`**

`src/data/skills.ts`，`sheshen_weizhu` 之后：

```ts
  /**
   * 赏顺伐逆（贾充主战法·被动）：受到恢复效果时 75% 为友军全体恢复 65%；
   * 受到策略伤害时 75% 对伤害来源策略攻击 180%。两段均受谋略，成长率未知取基值。
   */
  shangshun_fani: {
    id: 'shangshun_fani',
    name: '赏顺伐逆',
    type: 'passive',
    timing: 'battle_start',
    range: 5,
    triggerRate: 1,
    targetMode: 'self',
    tags: ['heal', 'damage'],
    onHeal: {
      victim: 'self',
      rate: 0.75,
      applyTo: 'allies',
      output: [{ kind: 'heal', rate: 65, strategyScaled: false, growthRate: 0 }],
    },
    onHurt: {
      victim: 'self',
      rate: 0.75,
      damageKind: 'strategy',
      applyTo: 'source',
      sourceMaxDistance: 5,
      output: [{ kind: 'strategy_damage', rate: 180, strategyScaled: false, growthRate: 0 }],
    },
    output: [],
  },
```

被动 `battle_start`：`triggerPassiveSkills` 对空 `output` 应无害。若现实现会对空 output 仍 `executeSkillOutputs`，确认不会误伤；必要时与典韦一样依赖 `onHurt` 早退——读 `triggerPassiveSkills`：若无 onHurt 早退且会执行 output，贾充同时有 onHurt 则准备阶段不应结算 output。打开 `triggerPassiveSkills`：与指挥类似，有 `onHurt` 则只登记。若**没有**对 `onHeal` 的跳过，空 `output` 执行也是 no-op。不要在有 `onHeal` 时误把减伤/伤害跑掉。

- [ ] **Step 3: 挂槽**

`scripts/build_heroes_seed.mjs` 的 `SKILL_ID_BY_NAME` 增加：

```js
  赏顺伐逆: 'shangshun_fani',
```

Run: `node scripts/build_heroes_seed.mjs`

Expected: 生成 `scripts/seed_heroes.sql`，贾充行 `main_skill_id` 为 `shangshun_fani`。

`web/data/heroes.json` 里 `h708` 的 `"mainSkillId": ""` 改为 `"shangshun_fani"`（测试无 MySQL 时 `initHeroDB` 读这份 json）。

- [ ] **Step 4: 跑贾充测试**

Run: `npx vitest run tests/main_skills_b19.test.ts`

Expected: PASS

若「受恢复」那条 `heals.length` 不是 2：检查直构 ctx 无 `woundedMortality` 时 `recoverTroops` 能否把 5000 加回去；群体奶走 `executeSkillOutputs` 的 `targets` 是否包含自己。heal 段不要加 `targetSide:'ally'`（否则会按距离重选，覆盖传入的 allies 池）。

若普攻也反击：确认 `damageKind` 过滤已接到 `triggerOnHurt`。

- [ ] **Step 5: Commit**

跳过。

---

### Task 6: 全量回归

**Files:** 无新文件；可能 `tests/__snapshots__/golden.json`（仅当固定测试集事件结构变了）

- [ ] **Step 1: 类型检查**

Run: `npx tsc --noEmit`

Expected: PASS

- [ ] **Step 2: 相关单测**

Run: `npx vitest run tests/main_skills_b2.test.ts tests/main_skills_b14.test.ts tests/main_skills_b15.test.ts tests/main_skills_b19.test.ts`

Expected: PASS（盲侯/典韦 onHurt 行为不变）

- [ ] **Step 3: 全量 + golden**

Run: `npx vitest run`

Expected: PASS。若 golden 仅因于禁受击多了 `status_inflicted` / `skill_cast` 失败：确认是固定测试集里真有于禁再删快照重生。无于禁的对局不应变。

- [ ] **Step 4: Commit**

跳过。

---

## Spec 覆盖自检

| Spec 条目 | 任务 |
|-----------|------|
| applyDamage 伤害类型、DoT=strategy、分兵=physical | Task 2 |
| OnHurt damageKind / 钩子 output | Task 1–2 |
| OnHeal + resolvingHealHooks | Task 4 |
| 防御缩放 0.026、80→8%、172→10% | Task 2–3 |
| 于禁补全 range 5、致死仍叠 | Task 2–3 |
| 贾充取基值 65/180、75% 士气、距离 5 | Task 5 |
| 挂槽 h708 | Task 5 |
| 不编造成长率、不录入周泰等 | 全计划未包含 |

无 TBD。`executeSkillOutputs` 第 5 参已存在，不重复造轮子。
